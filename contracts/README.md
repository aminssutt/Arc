# /contracts

- **Owner:** simerugby (producer) + vgtray (Agent/Tool interfaces) — **FROZEN after phase 0**.
- **Goes here:** event schema, push payload, validation event, Agent/Tool interfaces (AgentInput/AgentOutput), mock fixtures (`mock_stream/`, `push_fixtures/`), `decisions.md`.
- **Does NOT go here:** implementation of any of the above — contracts are declarations only. Change process: PR + immediate Discord ping + producer AND consumers approve.
