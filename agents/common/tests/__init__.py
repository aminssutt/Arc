"""Test package for the shared agent layer (agents/common).

Test-only. No production code lives here; no real network calls are made
(the Vultr Vector Store API is mocked via httpx.MockTransport).
"""
