"""Arc agents package.

Package marker so agents import as ``agents.<lane>`` from the repo root
alongside ``contracts`` (e.g. ``from agents.validation import ValidationAgent``).
Lane split is documented in ``agents/README.md``.
"""
