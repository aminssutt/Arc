# /data

- **Owner:** shared (coordinated) — **one owner per file, announce on Discord before editing**. Schema (`schema.md`) is owned by aminssutt.
- **Goes here:** telecom corpus + scenario seeds — alarm dictionary, topology, -48V plant points, inventory, crew schedule, cost/SLA params, corpus manifest.
- **Does NOT go here:** loader code (`/backend`), frozen schemas for events/interfaces (`/contracts`), secrets/API keys (local `.env` only).
