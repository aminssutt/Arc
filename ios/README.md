# /ios

- **Owner:** daniwavy5032
- **Goes here:** SwiftUI app — push handling, detected-failures list, the human validation flow (the demo's human loop, never trimmed).
- **Does NOT go here:** the web app (`/frontend`), backend/push service (`/backend`), Apple secrets (`.p8`/provisioning live in local `.env`/keychain only).
